const _strings = {
    Internal: {
        Diagnostics: {
            AdditionalInfo: "Additional info:",
            InvalidLogType: "Invalid log type: '{0}'. Defaulting to type Info..."
        }
    }
};

export module PcResources {
    export const Strings = _strings;
}
